= link_to "Stronger", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      = link_to("Programs", programs_path)
%ul.nav.pull-right
  - if user_signed_in?
    %li
      = link_to("Logout #{current_user.name}", signout_path, :method=>'delete')
  - else
    %li
      = link_to('Sign up or Login', signin_path)